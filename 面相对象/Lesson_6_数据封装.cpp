//
// Created by edy on 2022/7/11.
//

#include "Lesson_6_数据封装.h"

/*
 * C++数据封装
 * 所有的C++程序都有以下俩个基本要素：
 * 1.程序语句(代码)：这是程序中执行动作的部分，它们被称为函数。
 * 2.程序数据：数据是程序的信息，会受到程序函数的影响。
 *
 * 封装是面相对象编程中的把数据和操作数据的函数绑定在一起的概念，这样能避免受到外界的干扰和误用，从而确保安全。
 * 数据封装引申出了另一个重要的概念，OOP，即数据隐藏。
 *
 * 数据封装是一种把数据和操作数据的函数捆绑在一起的机制，数据抽象是一种仅向用户暴露接口而把具体的实现细节隐藏的机制。
 * C++通过创建类来支持封装和数据隐藏（public、protected、private)。我们已经知道，类包含私有成员(private)、
 * 保护成员(protected)和公有成员(public)。默认情况下，在类中定义的所有项目都是私有的.
 *
 * 示例同数据抽象。
 */